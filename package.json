{
    "name": "qingkuai-prettier-plugin",
    "version": "1.0.0",
    "license": "MIT",
    "type": "module",
    "scripts": {
        "dev": "rollup -c -w",
        "listen:index": "npx tsx --watch src/index.ts",
        "build": "rimraf dist && npx tsc && rollup -c && rimraf dist/types"
    },
    "keywords": [
        "formatting",
        "qingkuai",
        "prettier"
    ],
    "dependencies": {
        "prettier": "^3.3.3"
    },
    "devDependencies": {
        "tsx": "^4.19.1",
        "rimraf": "^6.0.1",
        "rollup": "^4.24.0",
        "typescript": "^5.6.3",
        "rollup-plugin-dts": "^6.1.1",
        "rollup-plugin-esbuild": "^6.1.1"
    },
    "description": "prettier plugin for formatting .qk file"
}
